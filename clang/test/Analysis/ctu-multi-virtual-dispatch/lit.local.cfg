config.substitutions.insert(
    0,
    (
        "%analyzer-emit-ast",
        "%clang -emit-ast -D__clang_analyzer__ -Xclang -fanalyzer-emit-direct-overriders",
    ),
)
config.substitutions.append(
    (
        "%direct-overriders-merger.py",
        '"%s" %s'
        % (
            config.python_executable,
            os.path.join(
                config.clang_src_dir, "utils", "analyzer", "direct-overriders-merger.py"
            ),
        ),
    )
)
